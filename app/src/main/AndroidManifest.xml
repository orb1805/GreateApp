<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="e.roman.greateapp">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ExtrasActivity"></activity>
        <activity android:name=".EventActivity" />
        <activity android:name=".EventCreatorActivity" />
        <activity android:name=".MyEventsActivity" />
        <activity
            android:name=".MainScreenScrollActivity"
            android:label="@string/title_activity_main_screen_scroll"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RegLogActivity" />
        <activity android:name=".RegUnivActivity" />
        <activity android:name=".RegNameActivity" />
        <activity android:name=".PersonActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainScreenActivity" />
        <activity android:name=".RegActivity" />
        <activity android:name=".LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>