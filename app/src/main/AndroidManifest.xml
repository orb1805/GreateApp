<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="e.roman.greateapp">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.PersonViewerActivity" />
        <activity android:name=".activities.FriendsListActivity" />
        <activity android:name=".activities.ExtrasActivity" />
        <activity android:name=".activities.EventActivity" />
        <activity android:name=".activities.EventCreatorActivity" />
        <activity android:name=".activities.MyEventsActivity" />
        <activity android:name=".activities.RegLogActivity" />
        <activity android:name=".activities.RegUnivActivity" />
        <activity android:name=".activities.RegNameActivity" />
        <activity android:name=".activities.PersonActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MainScreenActivity" />
        <activity android:name=".activities.RegActivity" />
        <activity android:name=".activities.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>